<header>
  <h1>Medirectory</h1>
  <p>Find a healthcare provider.</p>
  <div class="search-bar">
    {{#if basicSearch}}
      {{input value=query placeholder='Try "camille houston" or "williams portland"' class='search-bar' insert-newline="submitBasic"}}

      {{#link-to 'providers/index' (query-params q=query) tagName='button' class='search'}}Search it{{/link-to}}
      <br/>
      <button {{action 'advanced'}}> Advanced Search </button>
    {{else}}
      {{#advanced-search submit='submitAdvanced' name=name location=location taxonomy=taxonomy npi=npi}}
        {{#link-to 'advanced-search/index' (query-params name=name location=location taxonomy=taxonomy npi=npi) tagName='button' class='search'}}Search it{{/link-to}}
        <br/>
        <button {{action 'basic'}}> Basic Search </button>
      {{/advanced-search}}
    {{/if}}
  </div>
</header>

<div class="about">
  <h2>What is Medirectory?</h2>
  <p>Medirectory is a pilot project by The MITRE Corporation. The project is open source and its code can be accessed at <a href="https://github.com/Medirectory" alt="Source code for Medirectory">https://github.com/Medirectory</a>.</p>
</div>

{{outlet}}

{{!-- <div class="factoids">
  {{#each fact in model}}
  <aside><h1>{{fact.header}}</h1><p>{{fact.text}}</p></aside>
  {{/each}}
</div> --}}
