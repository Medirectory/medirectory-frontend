`import { formatIntegrationProfile } from '../../../helpers/format-integration-profile'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | format integration profile'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = formatIntegrationProfile 42
  assert.ok result
